import React from "react";

export const TestimonialCard = ({ testimonial }) => {
  // const { author, text, createdAt } = testimonial;
  return (
    <div className="testimonial-card">
      {/* <div>{author}</div>
      <div>{text}</div>
      <div>{createdAt}</div> */}
    </div>
  );
};
